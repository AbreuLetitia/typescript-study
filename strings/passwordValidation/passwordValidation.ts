/**
 * lets check if a password is valid or not.
 * password rules are:
 * 1. must have 12 digits
 * 2. first letter must be capital case
 * 3. must have at least
 */

const password: string = 'Oiiiitchau$@';